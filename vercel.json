{
   "version": 2,
   "regions": ["sin1"],
   "builds": [
      {
         "src": "server.js",
         "use": "@vercel/node",
         "config": { "memory": 512, "maxDuration": 10 }
      }
   ],
   "routes": [
      {
         "src": "/(.*)",
         "dest": "server.js"
      }
   ]
}
